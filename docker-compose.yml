version: "3.9"

services:

  backend:

    build: ./backend

    container_name: cafeteria-backend

    ports:
      - "5000:5000"

    environment:
      PORT: 5000
      MONGO_URI: mongodb://host.docker.internal:27017/cafeteria

    restart: always